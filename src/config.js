// src/config.js — single place to update the backend URL

const BACKEND = "https://web-production-bcb7.up.railway.app";

export const API_TRAFFIC   = `${BACKEND}/api/traffic`;
export const API_INCIDENTS = `${BACKEND}/api/incidents`;
export const API_AUTH      = `${BACKEND}/api/auth`;
export const API_BASE_ROOT = BACKEND;
